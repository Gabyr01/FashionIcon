import React from 'react'
import {PlacesAPI} from '../helpers/places.api'
const api = new PlacesAPI()
/**
 * TODO: Implement the search page
 * First create a SearchBar Component with a button in the components folder
 * then import in here. After the search bar component has been added 
 * Gaby and Ben will add a useEffect function that calls the api and populate this page. 
 */

function Search() {
    return (
        <div>
           /* Add the SearchBar Component here */ 
        </div>
    )
}

export default Search
