import React from 'react'

/**
 * FRONT End-Team
 * 
 * TODO: Create a separate branch and add page routing.
 * All the routes are defined inside the ./helpers/routing file
 * Import the file use those objects instead of hardcoding the paths.
 * It will helo keep the code clean and readable.
 */

function App() {
  return (
    <div className="App">
      hello world
    </div>
  );
}

export default App;
